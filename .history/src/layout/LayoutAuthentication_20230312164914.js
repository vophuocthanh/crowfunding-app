import React from "react";

const LayoutAuthentication = (props) => {
  const { children } = props;
  return <div className="w-full min-h-screen p-10 bg-lite">{children}</div>;
};

export default LayoutAuthentication;
